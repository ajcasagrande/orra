{
  "name": "pattern-${EDGE_OWNER}.${EDGE_DEPLOY}.${PATTERN_NAME}-${PATTERN_VERSION}",
  "label": "EdgeX Jakarta Services",
  "description": "Pattern for EdgeX Jakarta",
  "public": false,
  "services": [
    {
      "serviceUrl": "${EDGE_OWNER}.${EDGE_DEPLOY}.device-mqtt",
      "serviceOrgid": "${HZN_ORG_ID}",
      "serviceArch": "${ARCH}",
      "serviceVersions": [
        {
          "version": "${VERSION_device_mqtt}"
        }
      ]
    },
    {
      "serviceUrl": "${EDGE_OWNER}.${EDGE_DEPLOY}.device-onvif-camera",
      "serviceOrgid": "${HZN_ORG_ID}",
      "serviceArch": "${ARCH}",
      "serviceVersions": [
        {
          "version": "${VERSION_device_onvif_camera}"
        }
      ]
    },
    {
      "serviceUrl": "${EDGE_OWNER}.${EDGE_DEPLOY}.asc-mqtt",
      "serviceOrgid": "${HZN_ORG_ID}",
      "serviceArch": "${ARCH}",
      "serviceVersions": [
        {
          "version": "${VERSION_asc_mqtt}"
        }
      ]
    },
    {
      "serviceUrl": "${EDGE_OWNER}.${EDGE_DEPLOY}.edgex-ui",
      "serviceOrgid": "${HZN_ORG_ID}",
      "serviceArch": "${ARCH}",
      "serviceVersions": [
        {
          "version": "${VERSION_edgex_ui}"
        }
      ]
    },
    {
      "serviceUrl": "${EDGE_OWNER}.${EDGE_DEPLOY}.core-command",
      "serviceOrgid": "${HZN_ORG_ID}",
      "serviceArch": "${ARCH}",
      "serviceVersions": [
        {
          "version": "${VERSION_core_command}"
        }
      ]
    }
  ]
}
